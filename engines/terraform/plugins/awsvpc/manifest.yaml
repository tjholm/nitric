name: nitric-aws-vpc
version: 0.0.1
type: infra
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/nitric//plugins/awsvpc/module

inputs:

outputs:
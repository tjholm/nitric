name: nitric-aws-lambda
version: 0.0.1
type: service
deployment:
  # NOTE: // is required to reference a sub-path of repository for terraform modules
  terraform: github.com/nitrictech/nitric//plugins/service/awslambda/module
runtime:
  go_module: github.com/nitrictech/nitric/plugins/service/awslambda

inputs:

outputs: